# MINISHELL
MiniShell is a lightweight command-line interpreter that replicates basic Unix shell behavior. It allows users to execute commands, supports piping, input/output redirection, and built-in commands. Developed in C, it emphasizes process creation, command parsing, and system-level programming concepts.
